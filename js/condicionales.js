const disponible = 4000
const retirar = 200


if(disponible > retirar) {
    // se culmple la condicion 
    console.log('si puedes retirar ')
} else{
    // no se cumple la condicion 
    console.log('no puedes retirar')
}

/**
 * > Mayor que 
 * < Menor que
 * >= Mayor o igual
 * <= Menor o igual
 * == Igual
 * === Igual estricto
 * !== DIferente a
 */